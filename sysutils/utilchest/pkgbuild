#!/bin/sh
# Uncomment line 12 and comment lines 11, 15 and 19 for separated binaries.
NAME="utilchest"
VERSION="master"
LICENSE="UNLICENSE"
DESCRIPTION="A set of unix utilities written to be simple and small."
MAKEDEPS="libc"

SRC="$NAME"
GIT="https://github.com/eltanin-os/$NAME"
INFILE="../utilchest-box.in"
#INFILE="../utilchest.in"

. ${PORTS}/mk/indef.sh
. mk/box.sh

Prepare() {
#	( __default_prepare)
	( prepare_box )
}

$@
